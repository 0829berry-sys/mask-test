# 核心
streamlit>=1.53,<2.0
streamlit-drawable-canvas[image]>=0.12,<0.14
numpy>=1.24
opencv-python-headless>=4.9
Pillow>=10.0

# HEIC/HEIF 讀取支援（選用；不需要可移除，但上傳 .heic 檔時會失敗）
pillow-heif>=0.15

# SAM 分割引擎（若只使用 GrabCut 引擎可省略以下兩者）
torch>=2.1          # CPU 版即可；若有 NVIDIA GPU，改用對應 CUDA 版 (見 README)
segment-anything @ git+https://github.com/facebookresearch/segment-anything.git
